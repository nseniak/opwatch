#!/bin/sh
DIR=$(dirname $0)
cd ${DIR}

LOG_DIR=${DIR}/log
LOG_BASENAME=alerter
mkdir -p ${LOG_DIR} || exit 1
java \
	-Dapp.log.dir=${LOG_DIR} \
	-Dapp.log.basename=${LOG_BASENAME} \
	-jar lib/alerter-*.jar \
	"$@"
